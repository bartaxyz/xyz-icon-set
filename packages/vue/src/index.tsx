import Vue from 'vue';
import icons, { Icon, IconWrapper } from 'xyz-icon-set';

interface IconComponentsInterface {
    [key: string]: any; // TODO: Fix typings
};

const iconComponents: IconComponentsInterface = {}; // TODO: Fill up with vue components

export default {
    ...iconComponents,
};