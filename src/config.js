
const iconDirectory = './icons';
const pngIconDirectory = iconDirectory + '/png';
const svgIconDirectory = iconDirectory + '/svg';

export default {
    iconDirectory,
    pngIconDirectory,
    svgIconDirectory,
};
